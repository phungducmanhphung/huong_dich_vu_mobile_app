<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Client_khach_hang2"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.dang_nhap.DangNhapActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.test.TesterActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.cap_nhat_avatar_user.CapNhatAvatarUserActivitiy"
            android:exported="true" />
        <activity
            android:name=".ui.activities.main.MainActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.cap_nhat_thong_tin_user.CapNhatThongTinUserActivitiy"
            android:exported="true"
            android:theme="@style/Base.Theme.Client_khach_hang2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>